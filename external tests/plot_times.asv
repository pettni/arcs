data = load('run_times.txt');

test_count = 7;

semilogy(data(1, :), '-o';
hold on
semilogy(data(2, :), '-o');
semilogy(data(3, :), '-o');
semilogy(data(4, :), '-o');
hold off
xlabel('test id');
ylabel('win primal CPU time (s)');
legend('Log', 'Split', 'Reordered log', 'Reordered split', ...
      'Location', 'northwest');
grid on;